<% if params[:action] == 'dbsearch' %>
  <div class="container">
    <div class="row">
      <div class="col-sm-9">
        <%= bootstrap_flash %>
      </div>
    </div>
  </div>

  <% if @buffers.present? %>
   <%= render 'buffer' %>
  <% end %>

<% end %>
