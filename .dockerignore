# Git
.git
.gitignore
gitbranch

# Docker itself
Dockerfile
docker-compose.yml
.dockerignore

# Logs and temp files
log/*
tmp/*
!tmp/.keep

# Test coverage / CI artifacts
coverage/
.simplecov

# OS / editor noise
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Node (if any front-end tooling is present)
node_modules/

# Compiled assets (regenerated inside the container)
public/assets/
public/packs/

# Spring / Spring binstubs state
.spring/

# Secrets – never bake into the image
# config/secrets.yml       # kept because dev key is committed; remove for prod
# config/database.yml      # kept so the app can fall back if DATABASE_URL unset

# Documentation
doc/
README.md
